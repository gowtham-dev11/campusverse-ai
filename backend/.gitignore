node_modules/
.env
prisma/dev.db
prisma/dev.db-journal

# Vertex AI service-account key (if used instead of GEMINI_API_KEY) —
# never commit Google Cloud credentials.
service-account*.json
*-key.json
